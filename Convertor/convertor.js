const selectedBase = document.getElementById('selectedBase').value;
const enteredValue = document.getElementById('enteredValue').value;
const resultBase = document.getElementById('resultBase').value;
const button = document.getElementById('button');

let value = parseInt(enteredValue);
